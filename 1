function trigger() {
  var url = [
    "https://discord.com/oauth2/authorize?client_id=1242770299204665364"
  ];
  for(var x = 0; x<url.length; x++){
    var uri = url[x];
    try{
      UrlFetchApp.fetch(uri);
    }catch(err){Logger.log(err.message)}
    
  }
  
}
